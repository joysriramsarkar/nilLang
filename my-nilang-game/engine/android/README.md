# Android APK

Build and install a debug APK:

    nil build android
    adb install build/my-nilang-game.apk

The generated APK carries assets/app.nil and assets/app.nilax so the same game logic can run on device.
