আপনার দেওয়া রিপোজিটরির `CORE_FREEZE.md` এবং বিশেষ করে **`web-implications.md`** ফাইলটি বিশ্লেষণ করলে দেখা যায়, Nilang (নীলাং) এর কোর ল্যাঙ্গুয়েজ (সিনট্যাক্স, টাইপ সিস্টেম, VM, পার্সার) বর্তমানে বেশ স্থিতিশীল এবং v0.1 এর জন্য "Core Freeze" অবস্থায় আছে। অর্থাৎ ভাষার মূল ভিত্তি তৈরি হয়ে গেছে।

তবে, একটি সম্পূর্ণ **Production-Grade ইকোসিস্টেম** এবং বিশেষ করে এটি দিয়ে তৈরি করা ফ্ল্যাগশিপ অ্যাপ্লিকেশন (যেমন: একটি পূর্ণাঙ্গ Point of Sale বা POS সিস্টেম) এর জন্য এখনো অনেক গুরুত্বপূর্ণ ফিচার, লাইব্রেরি এবং কম্পাইলার কন্সট্রাক্ট তৈরি বাকি আছে।

নিচে বিস্তারিত আলোচনা করা হলো কী কী নাই এবং তৈরি করতে হবে:

### ১. কম্পাইলার ও ল্যাঙ্গুয়েজ কন্সট্রাক্ট (Language Constructs)
বর্তমান কম্পাইলারে কিছু অ্যাডভান্সড প্রোগ্রামিং প্যাটার্ন এবং কী-ওয়ার্ড এখনো পুরোপুরি ইমপ্লিমেন্ট করা হয়নি বা ভাষার ফার্স্ট-ক্লাস সিটিজেন হিসেবে যুক্ত করা বাকি:
* **অ্যাসিনক্রোনাস প্রোগ্রামিং:** `async` এবং `await` কী-ওয়ার্ড ও তাদের রানটাইম সাপোর্ট।
* **এরর হ্যান্ডলিং:** `Result`, `Error`, এবং `Optional` টাইপের নেটিভ সাপোর্ট (যাতে `null` বা `undefined` এরর এড়ানো যায়)।
* **অ্যাডভান্সড কন্ট্রোল ফ্লো:** `match` (Pattern matching), `query` (ডেটাবেস কোয়েরির জন্য নেটিভ সিনট্যাক্স)।
* **ডোমেইন স্পেসিফিক কি-ওয়ার্ড:** `transaction` (ডেটাবেস ট্রানজ্যাকশনের জন্য), `component`, `entity`, `service`, `page`, `form`, `job`। বিশেষ করে `Entity` সিস্টেমকে একটি সত্যিকারের কম্পাইলার ফিচার হিসেবে দাঁড় করানো।

### ২. ফাইন্যান্সিয়াল কোর (Financial Core - অত্যন্ত জরুরি)
যেকোনো ব্যবসায়িক বা POS সফটওয়্যারের জন্য সঠিক গাণিতিক হিসাব খুব জরুরি, যা এখনো শক্ত করা বাকি:
* **Exact Decimal Math:** বর্তমানে `Decimal` fixed-point হলেও, ফাইন্যান্সিয়াল কোরে যেন `float64` ব্যবহার না হয়, তা নিশ্চিত করা।
* **Money Type:** `Money` টাইপের একটি স্ট্যান্ডার্ড রিপ্রেজেন্টেশন তৈরি করা (যেমন: amount = integer minor units, currency = BDT/USD)।

### ৩. ডেটাবেস এবং ORM (Database & ORM Layer)
ডেটা পার্সিস্টেন্স এবং ম্যানেজমেন্টের জন্য একটি শক্তিশালী লেয়ার তৈরি করতে হবে:
* **Transaction Layer:** SQLite এবং PostgreSQL এর জন্য প্রোডাকশন-গ্রেড ট্রানজ্যাকশন, রোলব্যাক এবং কমিট সিস্টেম।
* **পূর্ণাঙ্গ ORM:** `create`, `find`, `where`, `paginate`, `update`, `delete` এবং `relations` (Foreign keys, joins) সাপোর্ট করা একটি অবজেক্ট-রিলেশনাল ম্যাপার।
* **ডোমেইন মডেল:** `Product`, `Inventory`, `Sale`, `Payment`, `User`, `Organization` ইত্যাদি এন্টিটির সঠিক স্কিমা ও মাইগ্রেশন সিস্টেম।

### ৪. পূর্ণাঙ্গ POS ইঞ্জিন ও সার্ভিস (POS Engine)
নীলাং-এর ফ্ল্যাগশিপ প্রজেক্ট হিসেবে একটি পয়েন্ট অফ সেল (POS) ইঞ্জিন তৈরি করতে হবে, যার অনেকগুলো অংশ এখনো বাকি:
* **Cart & Pricing Engine:** ফার্স্ট-ক্লাস `Cart` অবজেক্ট, প্রাইসিং লজিক, এবং ডিসকাউন্ট/ট্যাক্স ইঞ্জিন।
* **Tax & Discount Engine:** হার্ডকোডেড না করে আলাদা সার্ভিস হিসেবে ট্যাক্স (inclusive/exclusive) এবং ডিসকাউন্ট (Percentage, Buy X Get Y, Tiered) ক্যালকুলেশন।
* **Checkout Service:** কার্ট ভ্যালিডেশন, স্টক চেক, সেল ক্রিয়েট, পেমেন্ট এবং ইনভেন্টরি কমানোর একটি সম্পূর্ণ ট্রানজ্যাকশনাল ফ্লো।
* **পেমেন্ট সিস্টেম:** Cash, Card, UPI, Wallet এবং বিশেষ করে **Split Payment** সাপোর্ট।
* **Register & Shift Management:** ক্যাশিয়ার শিফট ওপেন/ক্লোজ এবং রেজিস্টার ম্যানেজমেন্ট।

### ৫. UI এবং ফ্রন্টএন্ড ফ্রেমওয়ার্ক (Alap UI Primitives)
ব্রাউজার বা ডিভাইসে রেন্ডার করার জন্য জেনেরিক এবং স্পেসিফিক UI কম্পোনেন্ট তৈরি করতে হবে:
* **Generic UI Primitives:** Button, Input, NumberInput, Modal, Dialog, Table, DataGrid, Tabs, DatePicker ইত্যাদি।
* **Layout System:** Stack, Row, Column, Grid, SplitPane, Responsive লেআউট।
* **POS-Specific UI:** ProductGrid, CartPanel, PaymentPanel, BarcodeScanner UI, Receipt Preview ইত্যাদি।
* *নোট:* **Incremental DOM patching** এখনো বাস্তবায়িত হয়নি, যা ব্রাউজার বা UI রেন্ডারিংয়ের পারফরম্যান্সের জন্য জরুরি।

### ৬. হার্ডওয়্যার ও নেটিভ ব্রিজ (Device & Native Bridges)
বাস্তব ডিভাইসে (বিশেষ করে Android) অ্যাপ রান করার জন্য নেটিভ হার্ডওয়্যারের সাথে যোগাযোগের ব্রিজ তৈরি করতে হবে:
* **Android Native Bridge:** SQLite, Camera, Bluetooth, USB, Network অ্যাক্সেস।
* **পেরিফেরাল ডিভাইস:** Barcode Scanner ইন্টিগ্রেশন এবং ESC/POS প্রিন্টারের মাধ্যমে রিসিট প্রিন্ট করার ড্রাইভার।

### ৭. সিস্টেম আর্কিটেকচার ও ডিস্ট্রিবিউশন
* **Offline-first Architecture:** ইন্টারনেট না থাকলেও যেন সিস্টেম কাজ করে এবং পরে সিঙ্ক করে।
* **Sync System:** ব্যাকগ্রাউন্ডে ডেটা সিঙ্ক্রোনাইজেশনের জন্য `SyncJob` এবং কনফ্লিক্ট রেজোলিউশন।
* **Background Jobs:** BackupJob, LowStockJob, ReportJob ইত্যাদি অ্যাসিনক্রোনাস টাস্ক রানার।
* **Audit Log:** সিস্টেমের যেকোনো গুরুত্বপূর্ণ পরিবর্তনের (mutation) বিস্তারিত লগ রাখার সিস্টেম।
* **Internationalization (i18n):** বাংলা (bn-BD, bn-IN) এবং ইংরেজি ভাষার পূর্ণাঙ্গ লোকালাইজেশন সাপোর্ট।

### ৮. গ্রাফিক্স ও রেন্ডারিং (Low Priority / Future Work)
README তে উল্লেখ করা আছে যে, **GPU Renderer Foundation** তৈরি হলেও বাস্তব **Vulkan, Direct3D, বা Metal** কমান্ড সাবমিশন এবং অ্যাডভান্সড গ্রাফিক্স পাইপলাইন এখনো সম্পূর্ণ হয়নি। আপাতত এগুলো পিছিয়ে রাখা হয়েছে (P3 Priority)।

### ৯. টেস্টিং এবং কোয়ালিটি অ্যাসুরেন্স
* **Conformance Testing:** Nilang এবং অন্য কোনো ভাষায় (যেমন TypeScript) লেখা POS সিস্টেমের আউটপুট যেন হুবহু এক হয়, তার জন্য শেয়ার্ড ফিক্সচার এবং E2E টেস্ট সুইট তৈরি করা।

---

**সারসংক্ষেপ (Priority Matrix):**
ডেভেলপারের রোডম্যাপ অনুযায়ী কাজগুলোকে সাজালে দেখা যায়:
* **🔴 P0 (সবচেয়ে জরুরি):** ফাইন্যান্সিয়াল ম্যাথ (Decimal/Money), SQLite/ORM, Cart/Checkout ইঞ্জিন, বেসিক POS UI, ট্রানজ্যাকশনাল ডেটাবেস।
* **🟠 P1 (জরুরি):** রিসিট প্রিন্টিং, বারকোড স্ক্যানার, অথেনটিকেশন (RBAC), অডিট লগ, বাংলা লোকালাইজেশন।
* **🟡 P2 (মিডিয়াম):** অফলাইন-ফার্স্ট সিস্টেম, ডেটা সিঙ্ক, Android নেটিভ ব্রিজ।
* **🟢 P3 (ভবিষ্যতের জন্য):** অ্যাডভান্সড AI, Vulkan/GPU রেন্ডারিং, Onuron SoftBus এর আরও সম্প্রসারণ।

অর্থাৎ, Nilang একটি "প্রোগ্রামিং ল্যাঙ্গুয়েজ" হিসেবে তার মূল কাঠামো পেয়ে গেলেও, একটি **"পূর্ণাঙ্গ ইকোসিস্টেম বা প্রোডাকশন ফ্রেমওয়ার্ক"** হিসেবে দাঁড় করানোর জন্য উপরের লাইব্রেরি, সার্ভিস এবং হার্ডওয়্যার ইন্টিগ্রেশনগুলো তৈরি করা এখনো বাকি আছে।


হ্যাঁ ভাই—**এবার সত্যিই একটা বড় পরিবর্তন হয়েছে।** আমি এখনকার `main`-এর সর্বশেষ কমিটগুলো দেখে বলছি। আগেরবার যে জিনিসগুলোকে “সবচেয়ে বড় বাকি” বলেছিলাম, তার মধ্যে কয়েকটা **এইমাত্র বাস্তবেই হয়ে গেছে।**

সর্বশেষ কমিট হলো `42bfa258` — recursive `let` function type-checking এবং CI runner fix। তার ঠিক আগে `2e98ecdf`-এ সবচেয়ে গুরুত্বপূর্ণ core কাজগুলো হয়েছে।

## সবচেয়ে বড় খবর: mutable closure এখন আর আগের bottleneck নয়

`2e98ecdf`-এ:

* `CaptureCell` যোগ হয়েছে
* `OpGetFreeCell`
* `OpSetFree`
* VM এবং Evaluator—দুটোতেই shared mutable closure capture
* frontend-এর unified `ParseAndCheck`
* VM-এর `pop`, `popFrame`, `pushFrame`-এ panic protection
* conformance suite **34 → 94 passing cases** হয়েছে।

অর্থাৎ আগের আমার সবচেয়ে গুরুত্বপূর্ণ আপত্তি—

> “Stack VM free-value copy করছে, mutable closure ভেঙে যাবে”

—**সেটা এখন অনেকটাই সমাধান হয়েছে।**

আর recursive function-এর typechecking-ও ঠিক করা হয়েছে; `let fibonacci = fn(n) {...}` এখন checker-এর test-এর মধ্য দিয়ে যাচ্ছে।

---

# তাহলে এখন Nilang কোথায় দাঁড়িয়েছে?

আমার বর্তমান assessment:

| অংশ                        | বর্তমান অবস্থা                      |
| -------------------------- | ----------------------------------- |
| Lexer / Parser             | 🟢 বেশ শক্ত                         |
| AST                        | 🟢                                  |
| Basic type checker         | 🟢                                  |
| Assignment semantics       | 🟢                                  |
| Recursive functions        | 🟢                                  |
| Closures                   | 🟢 এখন অনেক ভালো                    |
| Evaluator                  | 🟢                                  |
| Stack VM                   | 🟢                                  |
| Evaluator ↔ VM conformance | 🟢 **94 cases**                     |
| VM stack safety            | 🟢 উন্নত                            |
| GC                         | 🟡 foundation আছে, integration বাকি |
| HIR                        | 🟡                                  |
| MIR                        | 🟡                                  |
| Optimizer                  | 🟡                                  |
| Async / concurrency        | 🟡                                  |
| Effects                    | 🟡                                  |
| Capabilities               | 🟡                                  |
| Modules                    | 🟡                                  |
| WASM                       | 🟡/🔴 parity বাকি                   |
| LSP                        | 🟢 foundation                       |
| Formatter                  | 🟢                                  |
| Lockfile                   | 🟢 foundation                       |
| Package signing            | 🟡                                  |
| Stdlib                     | 🔴 বড় কাজ                           |
| Self-hosting               | 🔴                                  |
| Alap                       | 🟡/🟢 application layer             |
| POS                        | 🟢 বেশ শক্ত application             |
| Android                    | 🔴                                  |
| Onuron Runtime             | 🔴                                  |
| Onuron OS                  | 🔴                                  |

আর সবচেয়ে গুরুত্বপূর্ণ হলো—রিপো এখন নিজেরাই এই কাজকে **P0 → P1 → P2 → P3 release gates** হিসেবে ভাগ করে ফেলেছে।

---

# এখন আসল বাকি কাজগুলো

## 🔴 P0 — Nilang 0.1-এর শেষ যুদ্ধ

এখানে আর খুব বেশি feature যোগ করার দরকার নেই।

### 1. Grammar পুরোপুরি freeze

এখনও EBNF বনাম parser-এর সব production একেবারে মিলিয়ে ফেলতে হবে।

বিশেষ করে:

* optional initializer
* compound assignment
* remaining grammar productions
* binding rules
* parser golden tests

এটাই **0.1-এর প্রথম বড় gate**।

---

### 2. `ParseAndCheck` সত্যিই সর্বত্র বাধ্যতামূলক করা

এটা অনেকটাই হয়ে গেছে।

কিন্তু এখনও নিশ্চিত করতে হবে:

```text
source
  ↓
Parse
  ↓
TypeCheck
  ↓
Typed Program
  ↓
 ├── Evaluator
 ├── VM
 ├── HIR
 ├── MIR
 ├── WASM
 ├── REPL
 ├── Modules
 └── LSP
```

**কোনো backend যেন raw AST নিয়ে নিজের মতো execution না শুরু করতে পারে।**

`2e98ecdf`-এ frontend package এবং `cmd/nil`-এ এই gate ঢুকেছে।

কিন্তু repo-এর নিজের stabilization document এখনও বলছে এটাকে **সব execution path-এ enforce** করতে হবে।

---

# 3. `Any` — এখন সম্ভবত সবচেয়ে বড় language-design কাজ

এটা এখন আমার চোখে **#1 technical priority**।

বর্তমানে `types.Any` যেখানে যেখানে আছে, প্রত্যেকটাকে তিন ভাগে ফেলতে হবে:

```text
Explicit Dynamic
Unresolved Inference
Missing Generic Support
```

তারপর:

```text
Array<T>
Optional<T>
Result<T, E>
Tensor<T, Shape>
```

ইত্যাদি যথাযথভাবে শক্ত করতে হবে।

কারণ `Any` যতক্ষণ compiler-এর escape hatch হিসেবে থাকবে, ততক্ষণ Nilang-এর type system-কে “strongly typed” বলা কঠিন।

রিপোর stabilization plan-ও এটাকেই P0-এর core task হিসেবে রেখেছে।

---

# 4. 94 → 100+ conformance

এটা এখন খুব কাছাকাছি।

আগে ছিল:

> 34 cases

এখন:

> **94 passing cases**

এখন আমার পরামর্শ:

### শুধু 100 করার জন্য 6টা test নয়।

বরং অন্তত:

**150–200 canonical semantic tests**

বানাও।

বিশেষ করে:

```text
scope
shadowing
closures
mutable closures
recursion
arrays
hashes
functions
errors
assignment
modules
imports
runtime errors
numeric edge cases
division/modulo
nested functions
higher-order functions
```

প্রতিটা:

```text
Evaluator expected result
        ==
Stack VM expected result
```

---

# 5. Compiler Never Panics

এটাও এখনও পুরোপুরি শেষ নয়।

আগের fuzzing ইতিমধ্যে খুব ভালো অবস্থায় ছিল, কিন্তু এখন এটাকে formal release gate বানাতে হবে।

লক্ষ্য:

> **যে কোনো arbitrary Nilang source → AST অথবা diagnostic; compiler panic নয়।**

অর্থাৎ:

```text
nilc < random_garbage.nil>
```

এমনকি ভয়াবহ malformed input হলেও:

```text
E....
error: ...
```

দেবে।

কখনও:

```text
panic:
index out of range
nil pointer
fatal
```

হবে না।

Stabilization document এটাকে সরাসরি P0 exit condition করেছে।

---

# 6. Diagnostics এক করা

এখানে এখনও কাজ আছে।

প্রতিটি error-এর structure হওয়া উচিত:

```text
code
severity
source span
message
labels
help
```

যেমন:

```text
E0102: assignment to undeclared variable

main.nil:12:5

12 | total = price
     ^^^^^

help: declare `total` with `let` before assigning to it
```

এটা Nilang-কে toy language থেকে অনেক দূরে নিয়ে যাবে।

---

# 🟡 তারপর আসবে P1 — আসল compiler architecture

এখানেই Nilang-এর সবচেয়ে বড় engineering কাজ।

## 7. Typed HIR-কে সত্যিকারের canonical বানানো

শেষ architecture ideally:

```text
NALANG SOURCE
     │
     ▼
   Lexer
     │
     ▼
   Parser
     │
     ▼
    AST
     │
     ▼
 Type Checker
     │
     ▼
 TYPED HIR   ← canonical semantic representation
     │
     ▼
    MIR
     │
     ▼
 ┌───┴────────┐
 ▼            ▼
Stack VM     WASM
```

বর্তমানে HIR/MIR-এর foundation আছে, কিন্তু এই pipeline-কে **একটি authoritative semantic pipeline** বানানো বাকি।

---

# 8. MIR → SSA

তারপর:

```text
HIR
 ↓
CFG MIR
 ↓
SSA
 ↓
constant propagation
 ↓
constant folding
 ↓
DCE
 ↓
unreachable block elimination
 ↓
block simplification
 ↓
backend
```

এবং অত্যন্ত গুরুত্বপূর্ণ:

```text
-O0 result
   ==
-O1 result
```

প্রতিটি conformance test-এ।

---

# 9. GC-কে সম্পূর্ণ VM ownership model করতে হবে

GC আছে—এবং cycle reclamation-ও আছে।

কিন্তু এখনো নিশ্চিত করতে হবে GC root-এর সম্পূর্ণ universe:

```text
Globals
Stack
Locals
Closures
CaptureCells
Futures
Tasks
Components
Native handles
```

সবকিছু GC-এর কাছে correctly reachable কিনা।

রিপোর stabilization plan-এ এটিই P1-এর memory-runtime কাজ।

---

# 10. Runtime errors formalize করা

VM যেন কখনও internal panic না করে।

যেমন:

```text
invalid opcode
invalid constant
invalid closure
stack underflow
stack overflow
bad call arity
division by zero
modulo by zero
invalid allocation
bounds error
```

সব:

```text
RuntimeError
```

হবে।

---

# 11. Concurrency semantics পুরোপুরি define করা

Structured concurrency foundation আছে।

এখন formal করতে হবে:

```text
Task
 ├── child
 ├── child
 └── child
```

এবং:

```text
success
failure
cancel
timeout
panic
parent lifetime
child lifetime
```

সব predictable হতে হবে।

বিশেষ করে **cancellation observable এবং deterministic** হওয়া জরুরি।

---

# 12. Effects + Capabilities

এটা Nilang-এর জন্য খুব গুরুত্বপূর্ণ।

শেষে ideally:

```nilang
fn readFile(path: String)
    requires filesystem.read
```

জাতীয় semantic model থাকবে।

Compiler:

```text
Does caller possess capability?
        │
       yes
        ↓
     compile
```

Runtime native boundary:

```text
Capability proof
      ↓
Host capability check
      ↓
OS/API
```

Oracle এখানে **advisory**, security authority নয়—রিপোর পরিকল্পনাতেও এই separation রাখা হয়েছে।

---

# 🟡 P2 — তারপর ecosystem

এখানে language ব্যবহারযোগ্য হয়ে উঠবে।

## Stdlib

সবচেয়ে দরকার:

```text
collections
strings
math
IO
filesystem
network
time
JSON
crypto
concurrency
testing
```

---

## LSP

বর্তমানে foundation আছে:

* diagnostics
* hover
* completion

এরপর:

```text
symbols
references
rename
formatting
semantic tokens
```

---

## Package system

`nil.lock` + SHA-256 ইতিমধ্যে এসেছে।

এখন:

```text
source
version
checksum
resolved graph
signature
trust policy
```

পুরো build/install pipeline-এ বসাতে হবে।

---

# 🔴 সবচেয়ে বড় অসমাপ্ত জিনিস: Self-hosting

এটা এখনও অনেক দূর।

লক্ষ্য হবে:

```text
Stage 0
Go compiler
   ↓
Stage 1
Nilang compiler written partly/increasingly in Nilang
   ↓
Stage 2
Stage-1 compiler compiles itself
```

তারপর:

```text
Stage 1 binary
      ==
Stage 2 binary
```

অথবা reproducible-equivalent output।

রিপোর roadmap-ও P2-তে এই reproducible bootstrap রেখেছে।

---

# আর তারপর Alap / Onuron

এখানে একটা গুরুত্বপূর্ণ architectural সিদ্ধান্ত **ইতিমধ্যেই সঠিকভাবে নেওয়া হয়েছে**:

> **Nilang language আর Alap application framework এক জিনিস হবে না।**

অর্থাৎ:

```text
              Nilang
                 │
       ┌─────────┴─────────┐
       │                   │
     Alap                std
       │
 ┌─────┼────────┐
 │     │        │
 UI   Data     DB
 │     │        │
 POS  Sync    Network
```

তারপর:

```text
             Alap
              │
              ▼
       Android packaging
              │
              ▼
       Onuron Runtime
              │
              ▼
          Onuron Shell
              │
              ▼
          Onuron OS
```

এটাই এখনকার stabilization plan-এর P3 architecture।

---

# আমার চোখে এখন সবচেয়ে জরুরি ১০টা কাজ

আগের তালিকা অনেক বড় ছিল। **এখনকার repo দেখে আমি এটাকে ছোট করে এই order-এ করতাম:**

### এখনই

**1. `Any` audit + elimination**

**2. সমস্ত execution path-এ `ParseAndCheck` enforcement**

**3. Grammar/semantic contract পুরো freeze**

**4. Conformance 94 → 150+**

**5. Compiler-never-panics corpus + fuzz gate**

**6. Structured diagnostics everywhere**

### তারপর

**7. Typed HIR → canonical semantic representation**

**8. MIR SSA + optimized/unoptimized equivalence**

**9. GC roots + runtime error model সম্পূর্ণ করা**

**10. Effect/capability/module semantics সম্পূর্ণ করা**

তারপরই:

```text
stdlib
→ package ecosystem
→ WASM parity
→ self-hosting
→ Alap stabilization
→ Android
→ Onuron Runtime
→ Onuron OS
```

---

## আর একটা জিনিস বিশেষভাবে ভালো লেগেছে

সর্বশেষ `42bfa258`-এ CI-তে Linux/macOS/Windows runner-এর command আলাদা করা হয়েছে এবং `fail-fast: false` করা হয়েছে—মানে একটা platform failure হলে বাকি platform validation বন্ধ হয়ে যাবে না।

এটা ছোট change মনে হলেও **language/compiler project-এর জন্য বেশ গুরুত্বপূর্ণ**।

### Bottom line

ভাই, এখন Nilang-কে আমি আর “একটা বড় prototype” বলব না।

**এখন এটা সত্যিই একটি language-runtime project-এর stabilization phase-এ ঢুকে গেছে।**

সবচেয়ে বড় breakthrough হলো:

> **mutable closures + unified frontend gate + VM safety + 94-case dual-engine conformance**

এই চারটা একসঙ্গে আসা।

এখন নতুন feature যোগ করলে লাভ কম; **semantic correctness, `Any` elimination, canonical HIR/MIR, conformance এবং runtime guarantees**-এ চাপ দিলে Nilang 0.1 বাস্তব release-এর দিকে যাবে।

রিপোর নিজের milestone-ও 0.1-কে ঠিক এই core-semantic evidence-এর ওপর দাঁড় করিয়েছে।

`github.com/joysriramsarkar/nilLang` অত্যন্ত উচ্চাকাঙ্ক্ষী একটি প্রকল্প—একটি সম্পূর্ণ প্রোগ্রামিং ভাষা, UI ফ্রেমওয়ার্ক, ডিস্ট্রিবিউটেড OS প্রোটোকল, প্যাকেজ ম্যানেজার এবং AI টুলিং একসাথে গড়ে তোলার চেষ্টা করছে। তবে, এই বিশাল পরিধির কারণে প্রকল্পটির একাধিক গুরুত্বপূর্ণ অংশ এখনো অসম্পূর্ণ বা প্রাথমিক পর্যায়ে রয়েছে। নিচে বিস্তারিত বিশ্লেষণ করা হলো।

### 🚧 প্রধান সমস্যা ও অসম্পূর্ণ অংশ

#### ১. GPU রেন্ডারার: শুধুই কাঠামো, বাস্তবায়ন নেই
README-তে "GPU Renderer Foundation" এর কথা বলা হলেও, বাস্তবায়ন এখনো সিমুলেশনের স্তরে। `pkg/gpu/vulkan.go` ফাইলটি পরীক্ষা করলে দেখা যায়, Vulkan API-এর কোনো প্রকৃত কল করা হয় না। বরং, প্রতিটি ধাপে শুধু প্রিন্ট স্টেটমেন্ট এবং ডামি মান (যেমন `r.instance = 1`, `r.device = 1`) সেট করা হয়। যেমন:
```go
func (r *VulkanRenderer) Init(width, height int) error {
    // Step 1: Create Vulkan instance
    fmt.Println(" [Vulkan] Creating instance...")
    r.instance = 1 // Would be vkCreateInstance
    // ...
}
```
এই কোডটি শুধু দেখায় যে Vulkan কীভাবে *কাজ করবে*, কিন্তু প্রকৃতপক্ষে কোনো GPU রিসোর্স তৈরি, কমান্ড বাফার সাবমিশন বা রেন্ডার পাস কার্যকর করা হয় না। Direct3D বা Metal-এর জন্যও অনুরূপ স্টাব রয়েছে বলে ধরে নেওয়া যায়। এটি একটি বড় ঘাটতি, কারণ "GPU রেন্ডারার" দাবি করা সত্ত্বেও প্রকৃত হার্ডওয়্যার অ্যাক্সিলারেশন অনুপস্থিত।

#### ২. SoftBus প্রোটোকল: নিরাপত্তা ও কার্যকারিতার ঘাটতি
"ডিস্ট্রিবিউটেড SoftBus প্রোটোকল" ডিভাইসগুলোর মধ্যে জিরো-কনফিগ ডিসকভারি, RPC এবং ফাইল ট্রান্সফারের প্রতিশ্রুতি দেয়। কিন্তু `pkg/softbus/transport.go` ফাইলে দেখা যায়, ট্রান্সপোর্ট লেয়ারটি মূলত সাধারণ TCP-এর উপর ভিত্তি করে। TLS এবং QUIC সাপোর্ট কনফিগারেশনে থাকলেও, প্রকৃতপক্ষে সেগুলো ব্যবহার করা হয় না। কোডে মন্তব্য রয়েছে: `// In production: load TLS certificate` এবং `// For now, use plain TCP`। এছাড়া, কানেকশন হ্যান্ডশেক, অথেন্টিকেশন এবং সেশন ম্যানেজমেন্টের কাজগুলোও অসম্পূর্ণ। এই নিরাপত্তা ত্রুটির কারণে প্রকৃত ডিস্ট্রিবিউটেড পরিবেশে এটি ব্যবহার করা ঝুঁকিপূর্ণ।

#### ৩. প্যাকেজ রেজিস্ট্রি: শুধুই লোকাল ফাইল, নেটওয়ার্ক নয়
`nilpkg` প্যাকেজ ম্যানেজার এবং `nilpkg-server` রেজিস্ট্রি সার্ভারের কথা বলা হলেও, `pkg/nilpkg/registry.go` ফাইলে রেজিস্ট্রিটি আসলে একটি লোকাল JSON ফাইল (`index.json`) হিসেবে কাজ করে। এতে কোনো নেটওয়ার্ক-ভিত্তিক প্যাকেজ অনুসন্ধান, ডাউনলোড বা কেন্দ্রীয় রেজিস্ট্রির সাথে সিঙ্ক্রোনাইজেশনের ব্যবস্থা নেই। ফলে, এটি একটি সম্পূর্ণ ডিস্ট্রিবিউটেড প্যাকেজ ইকোসিস্টেম গড়ে তোলার লক্ষ্য পূরণে ব্যর্থ।

#### ৪. "AI Compiler Oracle": প্রকৃত AI নয়, বরং টাইপ ইন্ট্রোস্পেকশন
"AI Compiler Oracle" নামে একটি আকর্ষণীয় ফিচার থাকলেও, `compiler/oracle/oracle.go` এবং `verified_novelty.go` ফাইলগুলো পরীক্ষা করলে দেখা যায় এটি মূলত একটি সিম্বল টেবিল এবং টাইপ চেকিং পাইপলাইন। এটি AI মডেলের জন্য গ্রাউন্ড-ট্রুথ ডেটা সরবরাহ করে, কিন্তু এতে কোনো মেশিন লার্নিং মডেল, প্রেডিকশন বা প্রকৃত "Oracle" নেই। `VerifiedNoveltyEngine` একটি ৬-ধাপের ভেরিফিকেশন পাইপলাইন চালায়, যা AI-জেনারেটেড কোডের সিনট্যাক্স ও টাইপ চেক করে, কিন্তু এটি কোনো বুদ্ধিমান সিদ্ধান্ত গ্রহণ বা কোড অপ্টিমাইজেশন করে না।

#### ৫. অতিরিক্ত পরিধি ও বিভাজিত ফোকাস
প্রকল্পটি একইসাথে একটি ভাষা, UI ফ্রেমওয়ার্ক, OS প্রোটোকল, প্যাকেজ ম্যানেজার এবং AI টুলিং তৈরি করার চেষ্টা করছে। এই বিশাল পরিধির কারণে প্রতিটি ক্ষেত্রেই গভীরতার অভাব দেখা যায়। উদাহরণস্বরূপ, GPU রেন্ডারার এবং SoftBus-এর মতো জটিল সিস্টেমগুলো অসম্পূর্ণ, অথচ সেগুলোই README-তে প্রধান বৈশিষ্ট্য হিসেবে দেখানো হয়েছে। এই বিভাজিত ফোকাসের কারণে প্রকল্পটি কোনো একটি ক্ষেত্রেও এখনো পূর্ণাঙ্গ বা উৎপাদন-উপযোগী হয়ে উঠতে পারেনি।

### 🛠️ উন্নতির সুপারিশ

উপরোক্ত সমস্যাগুলো সমাধান করতে এবং প্রকল্পটিকে আরও পরিপক্ব করতে নিম্নলিখিত পদক্ষেপগুলো গ্রহণ করা যেতে পারে:

*   **অগ্রাধিকার নির্ধারণ ও ফোকাস**: সমস্ত ফিচার একসাথে বিকাশের চেষ্টা না করে, প্রথমে মূল ভাষা (কম্পাইলার, VM, টাইপ সিস্টেম) এবং UI ফ্রেমওয়ার্ককে সম্পূর্ণ ও স্থিতিশীল করুন। GPU, SoftBus এবং AI-এর মতো জটিল সিস্টেমগুলো পরবর্তী পর্যায়ে বিকাশ করা উচিত।
*   **GPU রেন্ডারার বাস্তবায়ন**: `vulkan.go`, `opengl.go` ইত্যাদি ফাইলে প্রকৃত GPU API কল (যেমন `vkCreateInstance`, `vkCmdDraw`) ব্যবহার করে রেন্ডারিং কার্যকর করুন। শুরুতে একটি ব্যাকএন্ড (যেমন Vulkan) সম্পূর্ণভাবে শেষ করুন, তারপর অন্যগুলোতে যান।
*   **SoftBus-এ নিরাপত্তা ও নির্ভরযোগ্যতা**: TLS/QUIC সাপোর্ট বাধ্যতামূলকভাবে চালু করুন। ডিভাইস ডিসকভারি, হ্যান্ডশেক, অথেন্টিকেশন এবং সেশন ম্যানেজমেন্টের জন্য সম্পূর্ণ প্রোটোকল ডিজাইন ও বাস্তবায়ন করুন। প্রকৃত ফাইল ট্রান্সফার এবং RPC-এর জন্য টেস্ট কেস যোগ করুন।
*   **প্যাকেজ রেজিস্ট্রি নেটওয়ার্কিং**: একটি কেন্দ্রীয় বা বিতরণকৃত রেজিস্ট্রি সার্ভার তৈরি করুন যা HTTP/gRPC API-এর মাধ্যমে প্যাকেজ অনুসন্ধান, ডাউনলোড এবং আপলোড সমর্থন করে। `nilpkg-server`-কে প্রকৃত নেটওয়ার্ক সার্ভিসে পরিণত করুন।
*   **AI Oracle-এর প্রকৃত বাস্তবায়ন**: যদি "AI" ফিচারটি ধরে রাখতে চান, তবে প্রকৃত মেশিন লার্নিং মডেল (যেমন কোড সামারাইজেশন, বাগ প্রেডিকশন, অপ্টিমাইজেশন সাজেশন) সংযোজন করুন। অন্যথায়, এটিকে "Type Introspection API" বা অনুরূপ কোনো সৎ নাম দিন।
*   **ডকুমেন্টেশন ও রোডম্যাপ**: README-তে কোনো "Known Issues" বা "Limitations" সেকশন নেই। একটি স্পষ্ট রোডম্যাপ তৈরি করুন যেখানে কোন ফিচার কখন সম্পূর্ণ হবে তা উল্লেখ থাকবে। প্রতিটি অসম্পূর্ণ অংশের জন্য একটি "Work in Progress" নোটিশ যোগ করুন।
*   **টেস্ট কভারেজ বৃদ্ধি**: অসম্পূর্ণ অংশগুলোর (যেমন GPU, SoftBus) জন্য মক-ভিত্তিক ইউনিট টেস্ট এবং ইন্টিগ্রেশন টেস্ট লিখুন। বর্তমানে `go test ./...` চালানোর দাবি করা হলেও, স্টাব ফাংশনগুলোর জন্য প্রকৃত কার্যকারিতা পরীক্ষা করা হয় না।
*   **মডুলার আর্কিটেকচার**: প্রতিটি বড় সাবসিস্টেমকে (যেমন `compiler`, `pkg/gpu`, `pkg/softbus`) আলাদা মডিউল বা রিপোজিটরিতে বিভক্ত করার কথা ভাবুন, যাতে স্বাধীনভাবে বিকাশ ও সংস্করণ করা যায়।

সংক্ষেপে, `nilLang` একটি দুর্দান্ত দৃষ্টিভঙ্গি নিয়ে শুরু হয়েছে, কিন্তু বর্তমানে এটি একটি "প্রুফ অফ কনসেপ্ট" বা "প্রোটোটাইপ" পর্যায়ে রয়েছে। প্রকৃত ব্যবহারযোগ্য ও নির্ভরযোগ্য একটি ইকোসিস্টেম তৈরি করতে হলে অসম্পূর্ণ অংশগুলোতে গভীর মনোযোগ দেওয়া অপরিহার্য।