// Nilang Standard Library
// Module: std/json
//
// Standard JSON serialization and deserialization convenience module.

let encode = fn(value) {
    return native("std.json.encode", value);
};

let decode = fn(text) {
    return native("std.json.decode", text);
};
