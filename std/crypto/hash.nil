// Nilang Standard Library
// Module: std/crypto/hash
//
// Cryptographic hashing via host runtime.

let sha256 = fn(value) {
    return native("std.crypto.sha256", value);
};

let sha512 = fn(value) {
    return native("std.crypto.sha512", value);
};
