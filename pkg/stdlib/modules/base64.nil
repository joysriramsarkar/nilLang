// Nilang Standard Library
// Module: std/base64
//
// Base64 encoding and decoding convenience module.

let encode = fn(value) {
    return native("std.base64.encode", value);
};

let decode = fn(text) {
    return native("std.base64.decode", text);
};
