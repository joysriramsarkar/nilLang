// Nilang Standard Library
// Module: std/data/base64
//
// Base64 encoding and decoding via host runtime.

let encode = fn(value) {
    return native("std.base64.encode", value);
};

let decode = fn(text) {
    return native("std.base64.decode", text);
};
