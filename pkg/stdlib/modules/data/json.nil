// Nilang Standard Library
// Module: std/data/json
//
// Standard JSON serialization and deserialization via host runtime.

let encode = fn(value) {
    return native("std.json.encode", value);
};

let decode = fn(text) {
    return native("std.json.decode", text);
};
